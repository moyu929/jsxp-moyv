import { JSXPOptions } from './types';

/**
 * 定义 JSXP 配置
 * @param options - JSXP 工程配置选项
 * @returns 配置对象
 */
declare const defineConfig: (options: JSXPOptions) => JSXPOptions;

export { defineConfig };
